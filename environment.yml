# Conda environment file for Streamlit in Snowflake
# Specifies Python packages needed by the app

name: rcm_intelligence_hub
channels:
  - snowflake
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - snowflake-snowpark-python
  - streamlit

