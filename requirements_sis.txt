# RCM Intelligence Hub - Streamlit in Snowflake Dependencies
# Minimal requirements for SiS deployment

# Core dependencies (provided by Snowflake)
snowflake-snowpark-python>=1.11.0
streamlit>=1.28.0

# Note: The following are NOT needed for SiS (handled by Snowflake):
# - snowflake-connector-python (use get_active_session instead)
# - tiktoken (use built-in token estimation)
# - python-dateutil (included in Python stdlib)

